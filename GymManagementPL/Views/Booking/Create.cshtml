 CreateSessionViewModel
using GymManagementSystemBLL.ViewModels.SessionViewModels
<h1>Create New Booking</h1>

<hr />
<div class="row">
    <div class="d-flex justify-content-center align-items-center h-100">

        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">Booking Information</h5>
                </div>
                <div class="card-body">
                    <form method="post" asp-action="Create" asp-route-sessionId="">
                        <div class="mb-
                            <input hidden readonly asp-for="SessionId" value="" />
                            <label class="form-label">Member</label>
                            <input name="Phone" type="text" class="form-control" /> 
                            <select asp-for="MemberId" name="memberId" class="form-select" asp-items="">
                                <option value="">-- Select Member --</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i> Create Booking
                            </button>
                            <a asp-action="GetMembersForUpcomingSession" asp-route-id="" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> Back to Lis
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

    </div>
</di    <partial name="_ValidationScriptsPartial" />
    <partial name="_AlertBoxScripts" />
}
